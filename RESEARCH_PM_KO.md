# Research PM 0.6 - 검색·문서 선별 최적화

## 업데이트

1. PM을 중지하고 창을 완전히 종료한 뒤, `pm_data` 전체를 다른 위치에 복사해 백업하세요.
2. 업데이트 ZIP 내용을 기존 `START_RESEARCH_PM.bat`가 있는 프로그램 폴더에 덮어쓰세요. `src` 안에 다시 `src`를 넣지 마세요.
3. `pm_data`와 `.pm-venv`는 삭제하지 마세요. 기본 경로에 새 의존성을 추가하지 않았으므로, 0.5가 정상 실행되던 환경은 재설치가 필요하지 않습니다.
4. 기존 BAT로 실행하고 **Research PM 0.6**을 확인하세요.

첫 비교 설정: **DuckDuckGo / qwen3.5:9b / Context 8192 / Output 3072 / 균형 / Semantic OFF**.

## 기존 연구 이어가기

**0.6 방식으로 이어서 조사**를 누르면 같은 주제·지시로 새 0.6 프로젝트를 만들고 기존 연구를 명시적 참조로 연결합니다. 원래 DB는 변환하지 않습니다.

**선택 연구 재개**는 저장된 버전을 유지합니다. 0.5 연구는 0.5 엔진으로, 0.6 연구는 0.6 엔진으로 재개합니다. 0.4.x 연구는 읽기 전용입니다.

## 최적화 모드

| 모드 | 검색 변형 | 시도당 수집 상한 | 읽을 구간 우선순위 |
|---|---:|---:|---|
| 효율 우선 | 1 | 1 | 상위 2개 |
| 균형 | 1 | 3 | 상위 3개 + 필요 시 추가 2개 |
| 품질 우선 | 최대 3 | 3 | 상위 5개 + 필요 시 추가 5개 |

모드는 작업 예산을 바꾸는 설정이며, 사실 정확도를 보증하는 설정이 아닙니다.

## 원문과 보류 자료

수집한 원문은 보존하고 관련 구간을 먼저 읽습니다. 보류된 문서와 읽지 않은 구간은 사라지지 않으며, 추출 완료는 전체 사실을 빠짐없이 찾았다는 뜻이 아닙니다. 새 연구로 복사하지 않은 저순위 참조 원문은 기존 프로젝트에 남습니다.

## 선택 기능: Semantic

기본은 **OFF**입니다. CPU 백엔드는 별도로 준비된 SentenceTransformer 로컬 모델 폴더와 패키지가 필요하며, Ollama 백엔드는 이미 설치된 임베딩 모델을 사용합니다. 모델을 자동 다운로드하지 않습니다. 오류가 나면 어휘 기반 선별로 돌아갑니다. 실제 노트북 성능·메모리 비교는 아직 검증하지 않았습니다.

## 인계 및 비교

기존 handoff에 `efficiency_metrics.json`, `retrieval_plans.json`, `retrieval_decisions.csv/json`, `reference_decisions.json`, `pacing.json`, `chunk_rankings.jsonl`, `replay/`를 추가합니다. 실측 토큰이 없으면 null로 표시하며 추정치와 혼합하지 않습니다.

```text
python -m ollama_deep_researcher.pm_benchmark compare <v05-handoff> <v06-handoff> --output comparison.json
python -m ollama_deep_researcher.pm_benchmark validate-replay <handoff/replay>
```

**실제 Qwen 조사 품질과 30분/2시간 노트북 시험은 별도 검증 대상입니다.** 자동시험과 합성 자료 비교는 실제 조사 정확도나 처리속도의 보증이 아닙니다. `docs/RESEARCH_PM_V0_6_VALIDATION.md`를 참조하세요.

## 사용 방법

- 연구 주제와 추가 지시를 입력합니다. 기본 출처 정책은 **일반 웹 조사**이며 도메인 입력은 필수가 아닙니다.
- **참조 프로젝트 선택**에서 명시적으로 고른 연구만 참조합니다. 선택하지 않으면 완전히 독립적으로 시작합니다.
- 참조한 원문은 현재 프로젝트로 복사하고 출처 프로젝트 ID를 기록합니다. 기존 근거를 바로 정답으로 채택하지 않고 현재 질문에 맞게 재추출합니다. 참조의 참조를 자동 탐색하지 않습니다.
- 실행 시간은 기본 120분입니다. `0`은 **시간 제한만 없음**을 뜻하며, 고급 설정의 호출/검색/과제당 시도 한도는 유지됩니다.
- 일시정지 또는 사용자 중지 후에는 저장된 위치에서 재개합니다. 시간/작업 한도로 종료된 연구는 새 연구에서 참조로 선택하여 이어갈 수 있습니다.
- 보고는 지정 간격이 지난 뒤 **안전한 작업 경계**에서 쓰므로 모델 호출 중에는 지연될 수 있습니다. 작업 기록은 단계별로 DB에 저장됩니다.

## 저장 구조

```text
pm_data/
  backups/                       # 기존 DB/결과 백업
  migration.json                 # 이관 및 소속 불명 자료 기록
  projects/<project-id>/
    project.sqlite3              # 이 프로젝트만의 DB
    documents/<document-id>/     # original + text.txt
    evidence/                    # 이관된 예전 근거 보관
    logs/
    handoff/
      handoff.md
      evidence.json
      sources.csv
      source_records.json
      candidates.json
      unresolved.md
      processing.json
      events.jsonl
      manifest.json
      documents/
```

## ChatGPT로 인계

**결과 폴더**를 누르면 현재 자료로 인계 묶음을 갱신합니다. 정지된 상태에서 `handoff/` 폴더를 통째로 압축하여 전달하세요. 대형 자료는 수신 서비스의 업로드 한도에 맞게 나눠야 할 수 있습니다. 요약만 보내지 말고 원문도 함께 보내세요.

> 로컬 PM의 요약과 판정은 확정 사실이 아니다. 원문과 인용 위치를 기준으로 재검토하고, 단일 출처·검토 미완료·충돌 후보를 구분하라. 후보 자료를 검증된 결론으로 혼합하지 말라.

`manifest.json`의 경로와 SHA-256으로 파일 완전성을 확인할 수 있습니다. 인용 위치는 `text.txt`의 **문자 인덱스**이며, PDF 페이지는 1부터 시작합니다. 0.6의 `events.jsonl`은 전체 이벤트를 내보냅니다. 0.5 이하의 기존 내보내기는 최근 500개로 제한되며 전체 이벤트는 프로젝트 DB에 남습니다.

## 판정과 한계

출처가 하나여도 저장합니다. Critic이 실패해도 원문을 삭제하지 않습니다. `NO_NEW_WORK`는 현재 한도와 검색 전략에서 새로 시도할 작업이 없다는 의미이지, 조사가 완벽하다는 의미가 아닙니다. 화면의 출처 보강 목표는 기본적으로 저장 제외 기준이 아닙니다. 엄격 최종 필터는 고급 설정에서 켤을 때 초안에만 적용됩니다.

- HTML/평문/텍스트 PDF 수집 지원. 스캔 PDF OCR·복잡한 표의 시각 해석·로그인 우회·JavaScript 브라우저 렌더링은 지원하지 않습니다.
- 원본은 기본 최대 20 MB, 추출 텍스트는 최대 200만 문자, PDF는 최대 300페이지입니다. 부분 추출/파싱 실패는 표시하고, 확보한 원본은 보존합니다.
- PDF 파싱은 별도 프로세스에서 30초 한도로 실행합니다. Unix에서는 메모리 상한도 적용하지만 Windows에서는 같은 OS 메모리 제한을 보장하지 않습니다.
- 모델의 관련성 판단이나 인용 해석의 정확도는 보장하지 않습니다. 같은 로컬 모델의 다른 역할은 독립된 외부 검증이 아닙니다.
- 네트워크와 모델 실행은 일부 대기 구간에서 즉시 중지되지 않고 통신 타임아웃 또는 다음 확인 지점까지 기다릴 수 있습니다.

## 검증

`docs/RESEARCH_PM_V0_6_VALIDATION.md`에 자동 시험 범위와 실기기 확인 항목을 기록했습니다.
