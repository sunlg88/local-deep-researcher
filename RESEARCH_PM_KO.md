# Research PM 1차 개조판 — 먼저 읽기

기존 Local Deep Researcher는 보존하고, **한국어 GUI + 근거 중심 PM**을 별도 실행 모드로 추가했습니다.
같은 Qwen을 계획자·조사자·추출자·비판 검토자·작성자로 순차 호출합니다.
**5개 역할을 만들었다는 것이 석사 연구원 5명과 같은 성과를 검증했다는 뜻은 아닙니다.**

## 집 노트북에서 실행

1. GitHub에서 `feature/research-pm-phase1` 브랜치를 선택하고 **Code → Download ZIP**으로 받습니다.
2. 압축을 완전히 풉니다. Ollama를 켭니다. 이미 설치한 Qwen은 다시 설치하지 않습니다.
3. Python 3.11 이상과 Tkinter가 필요합니다. `INSTALL_PM_DEPENDENCIES.bat`를 한 번 실행합니다.
   이 파일은 전용 `.pm-venv` 환경을 만들고 프로젝트 의존성을 설치합니다. 인터넷 연결이 필요합니다.
4. `START_RESEARCH_PM.bat`를 실행합니다.
5. **연결 / 모델 찾기**에서 실제 설치된 모델명을 선택합니다. 기본값은 `qwen3.5:9b`입니다.
6. 기본 **출처 정책 = 일반 웹 조사**입니다. 이 모드에서는 도메인을 비워 두고 바로 시작해도 됩니다.
7. 업무 조사에서 공식 자료를 우선하고 싶으면 **신뢰 도메인 우선**을 선택하고 `example.org, example.net`처럼 도메인만 입력합니다.
8. 지정한 사이트 외에는 절대 읽지 않게 하려면 **허용 도메인 전용**을 선택합니다. 이 모드만 도메인 입력이 필수입니다.
9. 짧고 구체적인 연구 주제와 추가 지시를 입력하고 **새 연구 시작**을 누릅니다.

출처 정책:
- **일반 웹 조사**: 검색된 공개 HTTP(S) 웹페이지를 사용. 도메인 입력 불필요.
- **신뢰 도메인 우선**: 일반 웹도 사용하되 입력한 도메인의 검색 결과를 먼저 검토.
- **허용 도메인 전용**: 입력한 도메인의 결과만 검색·수집.

모든 모드에서 사설/루프백/예약 IP, 로컬 파일, 비표준 포트는 차단합니다.
첫 실행은 좁은 주제로 시험하세요. 미확인 자료는 억지로 채우지 않고 보류하는 것이 정상 동작입니다.

## 기본 설정

| 항목 | 기본값 |
|---|---:|
| 계획할 세부 과제 | 5~8개 |
| 과제별 최대 시도 | 3회 |
| 최소 출처 그룹 및 서로 다른 문서 본문 | 2개 |
| 전체 LLM 호출 한도 | 240회 |
| 전체 검색 한도 | 60회 |
| 컨텍스트 / 출력 한도 | 8,192 / 3,072 |
| 중간보고 주기 | 15분 |

한도는 GUI에서 바꿀 수 있습니다. 성능 예측치가 아니라 반복 폭주를 막는 안전장치입니다.
추론이 길어 출력 한도에 닿으면 불완전한 결과를 채택하지 않고 오류를 표시합니다.
컨텍스트나 출력 한도를 무작정 올리지 말고 실행 기록부터 확인하세요.
재개는 화면의 새 설정이 아니라 **해당 연구에 저장된 설정**을 사용합니다.

## PM이 실제로 하는 일

주제를 과제로 분해하고 완료 기준을 저장합니다. 과거 DB를 제한 검색한 뒤 부족한 자료를 검색합니다.
선택한 출처 정책을 만족하는 HTML/평문 원문에서 인용문과 수치를 추출하고, 원문에 존재하는 인용인지 검사합니다.
비판 검토자가 각 기준을 판정하고 Python이 출처·근거 ID·복제 본문·충돌 여부를 다시 검사합니다.
탈락하면 구체적인 이유를 다음 조사에 전달합니다. 통과한 과제는 반복하지 않습니다.
모든 과제의 결과를 합치되 보류 항목과 한계를 남깁니다.

‘채찍’은 근거 없는 결과의 반려와 구체적 재작업 지시이고, ‘당근’은 통과 과제를 종료하고 다음 과제로 이동하는 것입니다.
모델에 감정을 자극하거나 보상학습을 새로 수행하는 방식은 아닙니다.

## 상태와 결과물

- `QUOTE_CHECKED`: 인용문이 수집 본문에 존재함. 사실의 진실성이나 해석의 정확성을 보증하지 않음.
- `DONE / REVIEW_PASSED`: 자동 검사 통과. 독립 전문가 검증이 아님.
- `COMPLETED_REVIEW_REQUIRED`: 작업 절차 종료, 사람 검토 필요.
- `PARTIAL / BLOCKED`: 근거 부족·충돌·반복 실패로 부분 결과 또는 보류.
- `BUDGET_EXHAUSTED`: 설정한 작업 한도 도달. 완료 판정이 아님.

결과 폴더 버튼으로 `pm_data/projects/<연구 ID>/`를 엽니다.
`progress.md`는 진행 상황과 초안, `evidence_pack.json`은 원문·출처·근거·작업 상태 묶음입니다.
SQLite DB에는 이전 연구의 자료도 누적됩니다. 검색용 컨텍스트를 줄여도 누적 근거 연결은 삭제하지 않습니다.

일시정지와 중지는 요청으로 저장되며, 통신·추론 처리 경계에서 반영됩니다.
무응답 통신에서는 타임아웃을 기다릴 수 있고, GPU 연산이 즉시 중단된다는 보장은 없습니다.
프로그램을 다시 열면 저장된 연구를 선택해 재개합니다. 중단된 단계는 다시 실행될 수 있습니다.

## 범위와 주의사항

이번 단계는 HTML/평문 대상입니다. **PDF/OCR, XLSX 출력, 벡터 RAG, 자동 충돌 해소, 원격접속은 아직 없습니다.**
같은 모델의 비판 검토는 독립적인 검증이 아닙니다. 출처 도메인이 다르더라도 같은 원자료의 재인용일 수 있습니다.
검색 차단과 실제 자료 부재를 항상 구분할 수는 없습니다. 자세한 제약은 `docs/PHASE1_VALIDATION.md`를 참고하세요.

모델은 로컬이지만 검색어는 선택한 외부 검색 서비스로 전송됩니다.
Tavily는 별도 API 키와 서비스 비용이 발생할 수 있으며, SearXNG는 별도 인스턴스와 `SEARXNG_URL` 설정이 필요합니다.
새 GUI와 검색 워커는 LangSmith 추적을 끕니다. GPT로 자료를 자동 전송하지 않습니다.
`.env`, DB, 결과물은 git-ignore 대상이지만 강제 업로드는 막지 못합니다. 회사 기밀이나 API 키를 공개 저장소에 올리지 마세요.

## 확인한 것과 남은 것

Linux / Python 3.13.5 / Xvfb에서 **자동시험 52개 통과**, Python 3.11 문법 검사와 GUI 표시 확인을 수행했습니다.
Ollama 응답과 검색 결과는 시험용 가상 응답입니다. 실제 Qwen 연구 성능을 측정한 결과가 아닙니다.
**Windows 실행, 실제 Qwen·외부 검색 연동, VRAM·발열, 24시간 운전, 사람 연구원 대비 생산성은 별도 확인이 필요합니다.**

개발자 시험 명령: `PYTHONPATH=src python -m unittest discover -s tests -v`.
