# Research PM 0.5 - 원문 보존·검색 품질·공통 추출

## 0.5 업데이트

1. 기존 PM을 중지·종료한 뒤 `pm_data` 전체를 별도 복사하여 백업하세요.
2. 업데이트 ZIP의 내용을 `START_RESEARCH_PM.bat`가 있는 기존 프로그램 폴더에 덮어쓰세요. `src` 안에 또 `src`를 넣지 마세요.
3. `pm_data`와 `.pm-venv`는 삭제하지 마세요. 기존 0.4.1 환경에는 새 의존성을 추가하지 않았으므로 설치 BAT를 다시 실행할 필요가 없습니다.
4. 기존 실행 BAT로 열고 `Research PM 0.5` 표시를 확인하세요. 우선 Context **8192**, Output **3072**를 유지하세요.

## 기존 연구 이어가기

**0.4.x 연구는 읽기 전용입니다.** 기존 연구를 선택하고 **0.5 방식으로 이어서 조사**를 누르면 같은 주제·지시로 새 연구를 만들고 기존 연구를 명시적 참조로 연결합니다. 기존 DB를 0.5 구조로 덮어쓰지 않습니다.

0.5에서 만든 연구는 일시정지/중지 후 **선택 연구 재개**로 이어갑니다. 구버전 내보내기는 `pm_data/legacy_exports/<project-id>/`에 저장됩니다. 전환 전 다른 창의 구버전 PM도 종료하세요.

## 주요 변경

- Planner가 만든 과제는 고정됩니다. Researcher는 검색 전략을 바꾸되 과제를 계속 추가하지 않습니다.
- 검색 결과의 URL/제목/요약을 선별하고 `site:` 조건을 다시 확인합니다. 약한 후보는 삭제하지 않고 보류 사유를 남깁니다.
- 한 문서 구간의 추출 결과를 여러 과제에 연결합니다. 과제별로 같은 구간을 다시 추출하지 않습니다. 실패 복구나 응답 저장 전 중단은 재호출이 필요할 수 있습니다.
- 추가 지시는 **4096 UTF-8 바이트**까지 입력하되, 설정과 과제를 포함한 입력 예산을 별도 확인합니다. 최대 길이 이하라도 설정에 따라 시작이 제한될 수 있습니다.
- 화면에 검색/수집/관련문서/근거/무성과 연속 횟수를 표시합니다. 추출 대기는 무성과로 세지 않습니다.
- 토큰 수는 여전히 추정치입니다. 실측 성공 표본으로 천천히 보정하며 기본 Context를 자동으로 늘리지 않습니다.

## 인계 파일

기존 묶음에 `search_attempts.csv/json`, `search_hits.csv/json`, `research_metrics.json`, `host_health.json`이 추가됩니다. 과제별 근거 수에는 공유 근거가 포함되므로 단순 합계를 전체 근거 수로 보면 안 됩니다. 생산적 추출 비율은 정확도가 아닙니다.

**실제 Qwen 조사 정확도·외부 검색 품질·노트북 장시간 운전은 별도 검증 대상입니다.** 자동시험 통과는 원문 해석의 정답 보증이 아닙니다. `docs/RESEARCH_PM_V0_5_VALIDATION.md`에 시험 범위와 미검증 사항을 기록했습니다.

## 사용 방법

- 연구 주제와 추가 지시를 입력합니다. 기본 출처 정책은 **일반 웹 조사**이며 도메인 입력은 필수가 아닙니다.
- **참조 프로젝트 선택**에서 명시적으로 고른 연구만 참조합니다. 선택하지 않으면 완전히 독립적으로 시작합니다.
- 참조한 원문은 현재 프로젝트로 복사하고 출처 프로젝트 ID를 기록합니다. 기존 근거를 바로 정답으로 채택하지 않고 현재 질문에 맞게 재추출합니다. 참조의 참조를 자동 탐색하지 않습니다.
- 실행 시간은 기본 120분입니다. `0`은 **시간 제한만 없음**을 뜻하며, 고급 설정의 호출/검색/과제당 시도 한도는 유지됩니다.
- 일시정지 또는 사용자 중지 후에는 저장된 위치에서 재개합니다. 시간/작업 한도로 종료된 연구는 새 연구에서 참조로 선택하여 이어갈 수 있습니다.
- 보고는 지정 간격이 지난 뒤 **안전한 작업 경계**에서 쓰므로 모델 호출 중에는 지연될 수 있습니다. 작업 기록은 단계별로 DB에 저장됩니다.

## 저장 구조

```text
pm_data/
  backups/                       # 기존 DB/결과 백업
  migration.json                 # 이관 및 소속 불명 자료 기록
  projects/<project-id>/
    project.sqlite3              # 이 프로젝트만의 DB
    documents/<document-id>/     # original + text.txt
    evidence/                    # 이관된 예전 근거 보관
    logs/
    handoff/
      handoff.md
      evidence.json
      sources.csv
      source_records.json
      candidates.json
      unresolved.md
      processing.json
      events.jsonl
      manifest.json
      documents/
```

## ChatGPT로 인계

**결과 폴더**를 누르면 현재 자료로 인계 묶음을 갱신합니다. 정지된 상태에서 `handoff/` 폴더를 통째로 압축하여 전달하세요. 대형 자료는 수신 서비스의 업로드 한도에 맞게 나눠야 할 수 있습니다. 요약만 보내지 말고 원문도 함께 보내세요.

> 로컬 PM의 요약과 판정은 확정 사실이 아니다. 원문과 인용 위치를 기준으로 재검토하고, 단일 출처·검토 미완료·충돌 후보를 구분하라. 후보 자료를 검증된 결론으로 혼합하지 말라.

`manifest.json`의 경로와 SHA-256으로 파일 완전성을 확인할 수 있습니다. 인용 위치는 `text.txt`의 **문자 인덱스**이며, PDF 페이지는 1부터 시작합니다. `events.jsonl`은 최근 500개 이벤트이고 전체 이벤트는 프로젝트 DB에 남습니다.

## 판정과 한계

출처가 하나여도 저장합니다. Critic이 실패해도 원문을 삭제하지 않습니다. `NO_NEW_WORK`는 현재 한도와 검색 전략에서 새로 시도할 작업이 없다는 의미이지, 조사가 완벽하다는 의미가 아닙니다. 화면의 출처 보강 목표는 기본적으로 저장 제외 기준이 아닙니다. 엄격 최종 필터는 고급 설정에서 켤을 때 초안에만 적용됩니다.

- HTML/평문/텍스트 PDF 수집 지원. 스캔 PDF OCR·복잡한 표의 시각 해석·로그인 우회·JavaScript 브라우저 렌더링은 지원하지 않습니다.
- 원본은 기본 최대 20 MB, 추출 텍스트는 최대 200만 문자, PDF는 최대 300페이지입니다. 부분 추출/파싱 실패는 표시하고, 확보한 원본은 보존합니다.
- PDF 파싱은 별도 프로세스에서 30초 한도로 실행합니다. Unix에서는 메모리 상한도 적용하지만 Windows에서는 같은 OS 메모리 제한을 보장하지 않습니다.
- 모델의 관련성 판단이나 인용 해석의 정확도는 보장하지 않습니다. 같은 로컬 모델의 다른 역할은 독립된 외부 검증이 아닙니다.
- 네트워크와 모델 실행은 일부 대기 구간에서 즉시 중지되지 않고 통신 타임아웃 또는 다음 확인 지점까지 기다릴 수 있습니다.

## 검증

`docs/RESEARCH_PM_V0_5_VALIDATION.md`에 자동 시험 범위와 실기기 확인 항목을 기록했습니다.
